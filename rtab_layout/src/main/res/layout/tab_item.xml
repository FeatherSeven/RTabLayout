<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <com.ruigu.tablayout.DrawableTextView
        android:id="@+id/dtv_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="10dp"
        android:width="40dp"
        android:height="40dp"
        android:text="tab"
        android:textSize="20sp" />

    <com.ruigu.tablayout.MsgView
        android:id="@+id/mv_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/dtv_msg"
        android:layout_alignEnd="@id/dtv_msg"
        android:layout_marginTop="-10dp"
        android:layout_marginEnd="-10dp"
        android:width="20dp"
        android:height="20dp"
        android:gravity="center"
        android:includeFontPadding="false"
        android:text="99"
        android:textColor="@color/white"
        app:mv_backgroundColor="#FD481F"
        app:mv_isRadiusHalfHeight="true" />

</RelativeLayout>